let username:string = "Jane"; //string
let hasLoggedIn:boolean = true; // boolean

// hasLoggedIn =+ " Doe";

console.log(hasLoggedIn);

const str:string[] = ["a", "b", "c"];
const num:number[] = [1, 2, 3]; 

interface Person {
    first: string,
    last: string,
}

const myPerson: Person = {
    first: "Jane",
    last: "Doe",
};


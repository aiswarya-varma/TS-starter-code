enum DirectionNum {
    Up = 1,
    Down,
    Left,
    Right,
}

enum DirectionStr {
    Up = "UP",
    Down = "DOWN",
    Left = "LEFT",
    Right = "RIGHT",
}

// enum E {
//     A = (() => 2)(),
//     B,
// }

enum HeterogeneousEnum {
    No = 0,
    Yes = "YES",
}